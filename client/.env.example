# Optional: override API URL if backend is elsewhere (default: http://localhost:8000)
# VITE_API_URL=http://localhost:8000

# Google Sign-In: create a Web application OAuth 2.0 Client ID at
# https://console.cloud.google.com/apis/credentials and add it here
# VITE_GOOGLE_CLIENT_ID=your_client_id.apps.googleusercontent.com
